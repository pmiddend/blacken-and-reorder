#!/usr/bin/env sh

curl --silent -XPOST "http://localhost:31338$(pwd)" --data-binary @- > /tmp/reordered

curl --silent -XPOST http://localhost:31337 --data-binary @- </tmp/reordered > /tmp/blackened

if [ -s /tmp/blackened ]; then
    cat /tmp/blackened
else
    cat /tmp/reordered
fi
